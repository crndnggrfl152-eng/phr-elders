<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>PHR for Elders — Personal Health Records</title>
  <meta name="description" content="PHR for Elders — printable booklet + offline-first app + community training." />
  <style>
    /* Basic reset and type */
    :root{
      --bg:#f7fafc;
      --card:#ffffff;
      --accent:#4f46e5; /* indigo-600 */
      --muted:#6b7280;
      --text:#0f172a;
      --radius:14px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background:var(--bg);
      color:var(--text);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.45;
    }
    a{color:inherit;text-decoration:none}
    .container{max-width:1100px;margin:0 auto;padding:20px}
    header{background:#fff;box-shadow:0 1px 4px rgba(16,24,40,0.04)}
    .header-inner{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:14px 20px}
    .brand{display:flex;align-items:center;gap:12px}
    .logo{
      width:44px;height:44px;border-radius:10px;background:var(--accent);
      color:#fff;display:flex;align-items:center;justify-content:center;font-weight:700;font-size:16px;
      box-shadow:0 6px 18px rgba(79,70,229,0.15)
    }
    .brand h1{margin:0;font-size:16px}
    .brand p{margin:0;font-size:12px;color:var(--muted)}
    nav.desktop{display:flex;gap:18px;align-items:center;font-size:14px}
    nav.desktop a{color:var(--muted)}
    .cta{padding:8px 14px;background:var(--accent);color:#fff;border-radius:10px;font-weight:600}
    /* hero */
    main{padding:28px 20px}
    .hero{display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center}
    @media (max-width:880px){ .hero{grid-template-columns:1fr;}}
    h2.hero-title{font-size:30px;margin:0;line-height:1.05;font-weight:800}
    p.lead{margin-top:12px;color:var(--muted);max-width:62ch}
    .hero-actions{margin-top:18px;display:flex;gap:12px;flex-wrap:wrap}
    .btn{display:inline-block;padding:10px 16px;border-radius:10px;font-weight:600;cursor:pointer}
    .btn.primary{background:var(--accent);color:#fff}
    .btn.ghost{border:1px solid rgba(79,70,229,0.12);color:var(--accent);background:transparent}
    /* promo card */
    .card{background:var(--card);border-radius:16px;padding:18px;box-shadow:0 6px 18px rgba(16,24,40,0.06)}
    .video-placeholder{height:220px;border-radius:10px;background:#f3f4f6;display:flex;align-items:center;justify-content:center;color:#9ca3af}
    .small{font-size:13px;color:var(--muted)}
    /* sections */
    section{margin-top:32px}
    .grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
    @media (max-width:880px){ .grid-3{grid-template-columns:1fr}}
    .feature{background:var(--card);padding:14px;border-radius:12px;box-shadow:0 6px 14px rgba(16,24,40,0.04)}
    /* how it works */
    ol.steps{list-style:none;padding:0;margin:0;display:grid;gap:12px}
    ol.steps li{background:var(--card);padding:12px;border-radius:10px;box-shadow:0 4px 12px rgba(16,24,40,0.04)}
    /* download / pilot */
    .two-col{display:grid;grid-template-columns:1fr 1fr;gap:16px}
    @media (max-width:880px){ .two-col{grid-template-columns:1fr}}
    /* contact */
    form{display:flex;flex-direction:column;gap:10px}
    input,textarea{padding:10px;border-radius:8px;border:1px solid #e6e7ea;font-size:14px}
    button[type="submit"]{padding:10px 12px;background:var(--accent);color:#fff;border-radius:8px;font-weight:700;border:0;cursor:pointer}
    /* footer */
    footer{margin-top:36px;padding:22px 0;color:var(--muted);text-align:center}
    /* mobile nav */
    .mobile-toggle{display:none}
    @media (max-width:880px){
      nav.desktop{display:none}
      .mobile-toggle{display:block}
      .mobile-menu{display:none;background:#fff;padding:12px;border-top:1px solid #eef2ff}
      .mobile-menu.show{display:block}
      .header-inner{padding:12px 16px}
    }
    /* small helpers */
    .muted{color:var(--muted)}
    .pill{display:inline-block;padding:6px 10px;border-radius:999px;background:#eef2ff;color:var(--accent);font-weight:600}
  </style>
</head>
<body>
  <header>
    <div class="container header-inner">
      <div class="brand">
        <div class="logo">PH</div>
        <div>
          <h1>PHR for Elders</h1>
          <p>Personal Health Records — offline-first, caregiver-friendly</p>
        </div>
      </div>

      <nav class="desktop" aria-label="Primary">
        <a href="#features">Features</a>
        <a href="#how">How it works</a>
        <a href="#download">Download</a>
        <a class="cta" href="#contact">Get Started</a>
      </nav>

      <div class="mobile-toggle">
        <button id="menuBtn" aria-expanded="false" aria-controls="mobileMenu" style="padding:8px;border-radius:8px;border:1px solid #e6e7ea;background:#fff;cursor:pointer">Menu</button>
      </div>
    </div>

    <div id="mobileMenu" class="mobile-menu container" role="navigation" aria-hidden="true">
      <a href="#features" style="display:block;padding:8px 0">Features</a>
      <a href="#how" style="display:block;padding:8px 0">How it works</a>
      <a href="#download" style="display:block;padding:8px 0">Download</a>
      <a href="#contact" class="pill" style="display:inline-block;margin-top:8px">Get Started</a>
    </div>
  </header>

  <main class="container">
    <section class="hero" aria-labelledby="hero-heading">
      <div>
        <h2 id="hero-heading" class="hero-title">Keep elders' health records with them — always.</h2>
        <p class="lead">A simple hybrid Personal Health Record (PHR) system built for low-connectivity communities: printable booklet + offline-first mobile app + community training for caregivers and barangay health workers.</p>

        <div class="hero-actions">
          <a class="btn primary" href="#download">Download App</a>
          <a class="btn ghost" href="#contact">Request Training</a>
        </div>

        <div style="margin-top:14px;font-size:13px;color:var(--muted)">
          <strong>30-second promo:</strong> "Help elders carry their history — not their paperwork."
        </div>
      </div>

      <aside class="card">
        <div class="video-placeholder" role="img" aria-label="Promo video placeholder">
          <div style="text-align:center">
            <div style="font-weight:700;margin-bottom:6px">Promo video / demo</div>
            <div class="small">Use this space for a 30-second demo video</div>
          </div>
        </div>
        <p class="small" style="margin-top:12px">Play the 30-second promo during community sessions or share on social media.</p>
      </aside>
    </section>

    <section id="features">
      <h3 style="font-size:20px;margin-bottom:10px">What makes PHR for Elders different</h3>
      <div class="grid-3">
        <div class="feature">
          <h4 style="margin:0 0 8px 0">Hybrid: Booklet + App</h4>
          <p class="small">A small printed booklet paired with an offline-first app — records work with or without internet.</p>
        </div>
        <div class="feature">
          <h4 style="margin:0 0 8px 0">Offline First</h4>
          <p class="small">Store data locally, sync when connectivity returns. Designed for rural barangays and clinics.</p>
        </div>
        <div class="feature">
          <h4 style="margin:0 0 8px 0">Community Training</h4>
          <p class="small">Workshops for barangay health workers and caregivers to keep records accurate and useful.</p>
        </div>
      </div>
    </section>

    <section id="how">
      <h3 style="font-size:20px;margin-bottom:10px">How it works</h3>
      <ol class="steps">
        <li>
          <h4 style="margin:0 0 6px 0">1. Fill the booklet</h4>
          <p class="small">Record history, meds, allergies, and vitals — easy fields and larger fonts for elders.</p>
        </li>
        <li>
          <h4 style="margin:0 0 6px 0">2. Scan or enter into app</h4>
          <p class="small">Barangay health workers or caregivers can scan a QR or type key information into the app for digital backup.</p>
        </li>
        <li>
          <h4 style="margin:0 0 6px 0">3. Sync when online</h4>
          <p class="small">Records sync to the cloud for continuity of care across providers when internet is available.</p>
        </li>
      </ol>
    </section>

    <section id="download" class="two-col">
      <div class="card">
        <h3 style="margin:0 0 8px 0">Get the app or order booklets</h3>
        <p class="small">APK for Android (offline-first). Printable booklet templates and facilitator guides available.</p>
        <div style="margin-top:12px;display:flex;gap:10px;flex-wrap:wrap">
          <a class="btn primary" href="#" onclick="alert('Download placeholder — replace with real link')">Download APK</a>
          <a class="btn ghost" href="#" onclick="alert('Order booklets placeholder — replace with real link')">Order Booklets</a>
        </div>
      </div>

      <div class="card">
        <h3 style="margin:0 0 8px 0">Pilot program</h3>
        <p class="small">We run 4-week pilots with barangay health centers: training, distributed booklets, and app onboarding.</p>
        <a class="pill" href="#contact" style="display:inline-block;margin-top:12px">Apply for pilot</a>
      </div>
    </section>

    <section id="contact">
      <h3 style="font-size:20px;margin-bottom:10px">Contact & Support</h3>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px">
        <form class="card" onsubmit="event.preventDefault(); alert('Thanks — message placeholder');">
          <div>
            <label class="small">Name</label>
            <input required type="text" placeholder="Your name" />
          </div>
          <div>
            <label class="small">Email</label>
            <input required type="email" placeholder="you@example.com" />
          </div>
          <div>
            <label class="small">Message</label>
            <textarea required rows="4" placeholder="How can we help?"></textarea>
          </div>
          <button type="submit">Send message</button>
        </form>

        <div class="card">
          <h4 style="margin:0 0 8px 0">Need fast help?</h4>
          <p class="small" style="margin:6px 0">Email: <a href="mailto:support@phr-elders.org">support@phr-elders.org</a></p>
          <p class="small" style="margin:6px 0">Phone: +63 912 345 6789</p>
          <p class="small" style="margin-top:12px">Follow us: <a href="#">Facebook</a> | <a href="#">YouTube</a> | <a href="#">Messenger</a></p>
        </div>
      </div>
    </section>

    <footer>
      <div id="siteYear">© <span id="year"></span> PHR for Elders — Designed for community care</div>
    </footer>
  </main>

  <script>
    // Mobile menu toggle
    const menuBtn = document.getElementById('menuBtn');
    const mobileMenu = document.getElementById('mobileMenu');
    menuBtn && menuBtn.addEventListener('click', () => {
      const showing = mobileMenu.classList.toggle('show');
      menuBtn.setAttribute('aria-expanded', showing ? 'true' : 'false');
      mobileMenu.setAttribute('aria-hidden', showing ? 'false' : 'true');
    });

    // Set the current year in footer
    document.getElementById('year').textContent = new Date().getFullYear();

    // Lightweight smooth scrolling for internal anchors
    document.querySelectorAll('a[href^="#"]').forEach(a => {
      a.addEventListener('click', function(e){
        const targetId = this.getAttribute('href').slice(1);
        if (!targetId) return;
        const target = document.getElementById(targetId);
        if (target) {
          e.preventDefault();
          target.scrollIntoView({behavior:'smooth',block:'start'});
          // close mobile menu if open
          if (mobileMenu.classList.contains('show')) {
            mobileMenu.classList.remove('show');
            menuBtn.setAttribute('aria-expanded','false');
            mobileMenu.setAttribute('aria-hidden','true');
          }
        }
      })
    });
  </script>
</body>
</html>
